const flights = require('../../mockDB/flights.json')


const searchByCity = (from, to) => {
  return async (dispatch) => {
    try {
      // const res = await axios.get(url)
    } catch (error) {
      
    }
  }
}